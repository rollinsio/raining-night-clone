# CHANGELOG

One line per module: what exists, and any known stubs. Folder ownership per ARCHITECTURE.md; the
skeleton build touched every folder once — later builders own their folders from here.

## Combat feel — swings that whip instead of smear (2026-08-28)

- `entity/Humanoid.js` — `Animator.play()` takes `blend` (seconds): crossfade from the interrupted pose, then apply the clip's curve verbatim (the old low-pass, `rate` 26 → τ ≈ 38 ms, was smearing 140 ms swings so the blade lagged the hitbox and kept moving into recovery). Low-pass mode unchanged for locomotion/idle. NEW `keyed()` builds a clip from key poses on a normalised clock (0..1 windup, 1..2 active, 2..3 recover) with per-key easing; bones a key omits carry over. `light1/2/3` and `heavy` re-authored as keys: chamber (arm folded, blade over the shoulder, torso wound), short creep/hold, `in`-eased acceleration into contact at 55 % of active, `out`-eased follow-through with overshoot, settle. Wrist deviation makes the blade trail the hand into the cut and lead through the follow-through; elbow extends at contact; hips/spine/chest lead the arm; overhead chops drop the body onto the front knee and end with the blade low ahead (not through the ground). Old `phase()` helper removed.
- `combat/Combat.js` — bladed humanoids hit-test the actual blade: grip→tip in world space (wrist bone), swept from last frame's segment (4 sub-steps × 5 points) against target cylinders; targets pressed against the attacker still count. Reach now follows the weapon (`TRAIL_SPAN`). Wolves/bosses keep the arc-sector path (`sweepArc`). Trail bound to `wristR` (the wrist rotates during swings now). Matrices refreshed before the sweep.
- `entity/Player.js`, `entity/Enemy.js` — attacks play with `blend` (70 / 100 ms); root-motion `step` lands as a bell-shaped lunge over the active frames (same distance, peak velocity into contact) instead of constant velocity.
- `combat/Pose.js` — contact point read off the wrist bone.

## Android conversion — portrait orientation (2026-08-28)

- `android/.../AndroidManifest.xml` — `sensorPortrait` (was `sensorLandscape`).
- `core/Touch.js` — overlay moved to its own root between `#hud` and `#menu` (outside `#hud` so the phone-size HUD zoom never shrinks hit targets; visibility mirrors `hud.visible` each frame); button/pad placement now comes from per-orientation tables (`LAYOUT.portrait` / `.landscape`) applied by `layout()` on build and resize — portrait puts the stick on the left half (50/50 pad split) with the ATK/HVY/ROLL/SKL/ULT/SPR arc up the right edge, clear of the bottom-centre art circles.
- `ui/HUD.js` — phone fit: uniform `zoom = min(1, w/730, h/730)` on `#hud` so the 1920×1080-proportioned layout (580 px bar band, 720 px map) fits phone screens in either orientation; 1920×1080 stays at zoom 1 so the capture/critic pipeline is pixel-identical; the reticle's screen-projected transform divides by `zoom`.
- `ui/Menus.js` — responsive panels below 730 px width (or 500 px height): single-column hub grid, `min-width` dropped, panel capped at 92vw/88vh with scroll, smaller type, `YOU DIED` at 52 px. In touch mode a first tap on a hub list name previews it and a second tap (or Begin) starts — no hover exists on phones.
- `tools/touch-smoke.mjs` — runs the full suite twice (412×915 portrait, 1280×720 landscape; 34 checks); button coordinates read from the live DOM so the orientation tables are what's exercised.

## Android conversion — Capacitor shell + touch controls (2026-08-28)

### core
- `core/Touch.js` — NEW: touch control overlay feeding the same named actions through Input, so no gameplay module changed. Dynamic virtual stick (left 44% of the screen, dead zone 0.14), camera look pad (right 56%, drag = orbit at 2.2× mouse gain, quick tap < 220 ms / < 12 px = lock-on toggle), labelled round buttons (ATK/HVY/ROLL/SKL/ULT, latching SPR that clears on stick release, contextual USE shown only while the HUD prompt is up, MAP + pause top-right). Dormant until a touch device is detected (coarse no-hover primary pointer at boot, or first touchstart); lives inside `#hud` so it hides with the HUD and stays under `#menu`. Cancelled pointerdowns suppress compatibility mouse events (no stray canvas attacks / pointer-lock asks); `setPointerCapture` is wrapped (synthetic events throw). Tap timing uses handler-time `performance.now()`, not `e.timeStamp` — synthesized pointerdowns carry stale stamps.
- `core/Input.js` — `touchAxis` merged into the move vector like the gamepad axis, `addLook(dx, dy)` (bypasses the pointer-lock gate), `noLock` flag so touch mode never requests pointer lock.
- `core/Game.js` — constructs `Touch` after the UI systems; calls `touch.update()` each frame (contextual USE button).
- Cross-folder: `ui/Menus.js` `resume()` skips `requestLock` when `input.noLock`; `src/main.js` wires the Capacitor back button (close map → toggle pause → minimize app; inert in browsers); `index.html` viewport locks scale + `viewport-fit=cover`, canvas gets `touch-action: none`. In touch mode `Touch.activate()` rewrites the HUD controls hint (touch wording, repositioned clear of the phone-height day/timer line and button cluster) and hides the keyboard keycap on the interact prompt — display-only writes through `hud.el`.

### build / android
- `vite.config.js` — NEW: `base: './'` so the bundle loads from the Capacitor WebView; `package.json` gains real scripts (`dev`, `build`, `preview`, `smoke`, `android:sync`, `android:apk`) and the Capacitor deps.
- `android/` — NEW: checked-in Capacitor 8 project (`io.rollins.slimveld`, compileSdk 36 / minSdk 24). `sensorLandscape` activity, immersive fullscreen (system bars hidden, swipe shows transiently), `FLAG_KEEP_SCREEN_ON`; `@capacitor/app` handles the hardware back button. Web assets under `android/app/src/main/assets/public` are generated by `cap sync` and gitignored.
- `capacitor.config.json` — app id/name, `webDir: dist`, dark background behind the WebView.

### tools
- `tools/touch-smoke.mjs` — NEW: 17 end-to-end checks over the touch path via CDP touch events (overlay activation + HUD-gated visibility, stick move/release, look drag, tap lock-on both ways, button edges, roll, sprint latch lifecycle, pause open + menu tap resume, zero console errors). Caps the frame loop (`setFps(1)`) because swiftshader renders < 2 fps and starves pointer handlers, which breaks tap-duration measurement.
- `tools/smoke.mjs` — unchanged and still green (27/27) on the desktop path.

## Gauntlet round 3 — vista & atmosphere (2026-08-21)

Critic gap (own read of the round-2 capture; the critic's frame was a black capture fault): flat overcast sky with no gradient and a moon that barely read, a dense carpet of identical ochre tufts, single-value olive mid-ground with no faceted rock, 1.1 M tris / 320 draw calls.

### render
- `render/Style.js` — sky keys retuned for a real gradient (`skyZenith` deep navy, `skyHorizon` / `mist` pale, `fog` a step under the horizon, `moon` near-white); new keys `fill` (cool fill light) and `shaft` (moonlight shafts); turf warmer (`grass`, `grassDark`, `grassPale`, `straw`), stone albedos a step lighter so facets step instead of sitting at black (`rockProp*`, `boulder*`, `crag*`), `grassTuftPale` added. No keys removed.
- `render/Atmosphere.js` — sky shader rebuilt: luminous horizon band (brighter on the moon's side) rising into a deep zenith, HDR moon disc (1.2, tone-maps white + blooms) with a three-stage halo, clouds reduced to thin low banks, stars hidden inside the halo; fog colour now follows the sky gradient in the view direction (band at the horizon, darker / bluer with elevation) so fogged ridges dissolve into the sky behind them, in-scatter toward the moon toned down; new shadowless cool `fill` DirectionalLight from behind the vista camera (camera-facing facets no longer share one backlit value); builds `Shafts` over the valley; projects the moon each frame into `postfx.setMoon()`.
- `render/Shafts.js` — NEW: one additive draw call of long soft moonlight beams (quads billboarded around their own foot→head axis, width / end fades, slow breathing), depth-tested so hills and trees occlude them; four feet in the misty valley below the overlook.
- `render/Postfx.js` — grade gains a touch of mid contrast; screen-space moon halo + faint downward ray fan around the projected moon (`uMoon`, aspect-corrected, fades as the moon leaves the frame); `setMoon(u, v, k)`.

### world
- `world/Terrain.js` — backdrop peaks lowered (hMax 175) and rim wall lowered (92 / 34) so the far crest sits inside the pale horizon band; slope→rock threshold lowered (0.22–0.42) for sheer faces on the spur; per-face jitter widened; flat turf facets get a slight warm cast (rock faces stay neutral).
- `world/Props.js` — NEW `cliff()` prop (row of tall tilted square-section slabs leaning together, tallest mid-row, scree at the foot; two variants) with `cliff` landmarks + a sparse steep-ground scatter; `ruinBit` landmarks (`arch` / `colTall` / `colShort` / `wall`) for explicit silhouettes; grass rebuilt as sparse clumps (3–7 tufts, 7/8/11-blade recipes, three quantised tints cool-olive / straw / pale-dry, smaller base scale, global cap 2800 tufts, meadow density r²/5) so bare turf shows between clumps; `shadeRock` side faces carry more value; scattered dead trees capped at 900; litter stones flatter. Vista: ~594 k tris incl. shadow + post (was 1.13 M), 320 draw calls.
- `world/Limveld.js` — vista dressing: faceted boulders at both frame edges instead of the old flanking pair, big boulders down the near slope, a sheer `cliff` band along the spur crest (~100 m) and a taller one on the far cliff band (~300 m), ruin fragments (arch, columns, wall) on the spur and the far band, fewer / smaller foreground litter stones, foreground grass grid denser but with small clumps (7 rows, 1.3 m spacing).

### notes for other owners
- `run/Ring.js`: the day-1 wall still shows as a pale blue-grey veil with faint rain streaks on the far left of the vista (behind the far ridge); a distance / fog fade of the wall body beyond ~500 m would clean that band.
- Capture tooling: while other builders edit, `tools/screenshot.mjs` can be reloaded mid-run by Vite HMR; this round used a scratch capture that swallows the `vite-hmr` websocket messages (not committed).

## Gauntlet round 3 — HUD & menus (2026-08-21)

### ui
- `ui/HUD.js` — matched to the Nightreign gameplay frames: bar tracks are a dark neutral well inside a light hairline (8/7/7 px), fills are muted brick / teal / moss that brighten to a cream tip; lag trails on every bar (cream HP damage, yellow stamina drain, pale-blue FP spend — `_trail` keys `hp/fp/st/boss`), `settleTrails(gap, stGap)` snaps all of them for poses; the flask row under the bars now shows (golden flask at 24×30 + count + separator + ult square; `out` ghost at 0) and the raised item slot carries the lit flask with its count; fire-pot glyph in the small sub-slot; rune counter band gets top + bottom hairlines with diamond end caps and a twelve-spoke rune wheel; ult art is a sword with rays, skill art a crescent slash; boss bar 7 px on a warm dark track with the cream trail; reticle slightly larger with a dark halo; roundel 88 px. Fixed: the ult square was absolutely positioned (the `SLOT` constant) so the flex row stacked it over the flask icon.
- Cross-folder: `entity/Player.js` — `flasks` / `maxFlasks` (3) in the constructor, refilled on `rest()` and `respawn()` (display-only stub: no drink input yet). `core/Debug.js` (hud pose only) — sets `p.flasks` when the Player lacks it; camera near-level (pitch −0.02, dist 6.4) so the whole Sentinel and its raised halberd sit under the moon with less floor in frame.

## Gauntlet round 3 — combat feel (2026-08-21)

Critic gap: the struck enemy was an undesigned mid-brown mannequin standing bolt upright on a same-value backdrop, so the spark burst read as FX sprayed on a capsule.

### entity
- `entity/enemies/EnemyRig.js` — soldier re-kitted for value separation, not hue: crested kettle helm (domed crown, wide down-turned pale brim, bone fin crest with a serrated top, dark horsehair tail) over a charcoal mail aventail; ONE big three-lame pauldron with a bone trim ring on the shield shoulder (the sword shoulder keeps a leather cap + strap); charcoal tabard panels front and back with raised bone borders, hem tongues and a bone diamond emblem; quilted gambeson sleeves; pleated skirt with a bone hem band (skirt-weighted to the hips); greaves; shield face charcoal with a pale steel rim and a bone cross. Palette gains a `bone` key per colourway (derived from PALETTE.skin/moon via `mixHex`); knight crest fin now gold. `ENEMY_CLIPS` = humanoid clips + a harder `hit` and a held hit-stop `recoil` (torso bent back and rolled away from the cut, near arm flung wide, far arm thrown up, front leg braced, rear leg skidding on its toe; `ctx.param < 0` mirrors it). The rim hook gained a warm impact rim (`HIT_RIM` uniform: contact position + strength, written by Combat) — a thin fresnel edge toward the contact plus a faint wrap so the burst visibly wraps the struck body without bleaching it. Body material is DoubleSide (open aventail / skirt sectors).
- `entity/Enemy.js` — heavy blows (`hit.poise > 28`) play `recoil` when the rig has it, light hits the flinch; the `hit` state now exits after `anim.ctx.dur` instead of a fixed 0.32 s.

### combat
- `combat/Trail.js` — ribbon redrawn as a translucent crescent that brightens toward the blade tip with a crisp hot tip line; premultiplied colour with MAX blending (`CustomBlending` + `MaxEquation`) so the samples that bunch near the hand no longer pile into an additive blob.
- `combat/Combat.js` — `impact()` sets `HIT_RIM` (decays with the impact light); impact light 3.2 cd placed 0.9 m back / 0.6 m up; new `skidDust(x, y, z, dir, strength)` (low smear dragged from a sliding foot + a few lifted puffs); dust colour a step paler (dirt→fog 0.5) so puffs read on the trampled ground.
- `combat/Pose.js` — composition mirrored (`POSE.side`, +1 default): camera over the hero's LEFT shoulder, `light2` cut left → right with a chest roll baked through the trail samples so the flat sweep becomes a high → low diagonal; the struck soldier lands big, right of centre, in the held `recoil` (mirrored) with its chest on the blade, a hotter impact light + `HIT_RIM` anchored on the contact and skid dust from its rear foot; fire centred behind the hero as a backlight with the knight's heavy wind-up beside it as a silhouette and the wolf crouched left of the hero (the right tent's footprint swallows anything placed beside the soldier); pose-only trail life 0.11 s / span 0.08 s (restored by `afterPose`). `side: -1` reproduces the old right-shoulder layout.

### notes for other owners
- `tools/screenshot.mjs` still dies when a parallel edit reloads the page between the pose and the fps pass; this round's captures used a retrying scratch script (boot → pose → shot, up to 4 attempts).
- `entity/Humanoid.js`: the hero's light swings are flat horizontal sweeps, which read as a thin edge-on line from a low camera; the combat pose fakes a diagonal cut by rolling the chest bone during the bake — a real diagonal `light2` clip would give the same read in gameplay.

## Gauntlet round 3 — character & animation (2026-08-21)

Critic gap: hard-faceted blocks with no tonal gradation or occlusion between parts, untapered limbs, blade driving into the ground.

### entity
- `entity/Humanoid.js` — `RigBuilder.build(materials, opts)` gained two opt-in bakes (existing rigs unchanged): `smooth` = crease-aware area-weighted vertex normals over the merged body (faces within 60° average, so lofts shade round while caps / hems / blade edges / box corners stay hard) and `ao` = point-based occlusion (one two-sided disc emitter per triangle, disc-to-point form factor, uniform grid, ~50 ms for 10k verts) multiplied into the vertex colours, with a ground-contact darkening and a two-octave value mottle; `posed(geo, pose)` skins the merged geometry into a spread pose for the bake (arms out, legs apart — the hanging bind pose made every limb occlude its neighbour). Distribution reported in `geometry.userData.ao`. `createHumanoid` now builds with `smooth: 60` + AO (strength 0.62, r 0.3) and turns `flatShading` off on the body material (roughness 0.96; head 0.9), so the hero shades like a painted figure: armpits / inner thighs / under the hood, mantle and belt darken. Character shader hook retuned: rim 1.4 → 0.75 with a tighter fresnel (pow 3.2) weighted to the key-light side (no uniform plastic outline), fill 0.5, wrap 0.6. Limbs re-lofted at 10 sides with designed taper (deltoid 0.082 → wrist 0.029 ≈ 2.6×, hip 0.098 → ankle 0.046 ≈ 2×, calf belly, slimmer bracer). Tunic pulled 35 % toward `stone` and mantle 60 % toward `leather` with lower shade multipliers (muted against the world; true pixel values ~27/38/64 and ~75/68/52 in the capture), hands darker, hood interior darker with a longer drooping peak and a wider drape seated on the mantle. Run clip: the sword arm now carries the forearm level with the blade pitched to ~+16° in world space (accounting for the ~0.6 rad spine/chest/pivot lean) — tip 1.18 m up and 1.3 m ahead at the hero frame, never below 0.66 m through the cycle, clear of the legs. Cloak: sway moved into a GLSL function `cloakOffset()` so the lit pass derives smooth normals by finite differences (`flatShading` off — soft folds instead of crumpled facets); less level lift (0.27) and calmer folds, albedo 0.56 (the streaming cloak faces the moon and was reading as a white sheet); scarf 1.05. Steel weapon parts ×1.45 so the blade reads as a blade in the back-lit frame.
- `entity/Player.js`, `entity/Camera.js` — unchanged this round.

### notes for other owners
- `entity/enemies/EnemyRig.js`, `entity/enemies/Wolf.js`, `entity/bosses/*` — can adopt the same look with `rb.build(mats, { smooth: 60, ao: { strength: 0.62, radius: 0.3, gain: 1.3, pose: {...} } })` plus `flatShading: false` on the body material; nothing changes for them until they opt in.
- `tools/screenshot.mjs` — dies whenever a parallel builder's edit full-reloads the page (`window.__game` undefined at the `state` wait). Routing `**/@vite/client` to a stub module (no HMR client) makes captures immune; each load still fetches fresh modules. This round's capture used such a scratch script.

## Gauntlet round 3 — churches, forts & ruins (2026-08-21)

Critic gap: the church read as clean white cardboard — intact block with a flat slab roof, ruler-straight wall tops, three floating planks for a collapse, a plain prism tower, no side buttresses, one uniform light-grey value.

### world
- `world/Structures.js` — Kit shading: per-quad facet value jitter (`facet`, every 6-vertex quad of a non-indexed box gets its own ±5 %), stronger height AO (0.52 at the ground → 1 at 4.2 m), deeper damp/green base course (0.42 over 1.8 m), low-frequency `weather()` patches, `occluder()` boxes baked as inside-corner AO in `build()` (buttress / porch / tower junctions), `fire({ halo:false })` bakes warm light without registering an Atmosphere glow billboard (facade sconces no longer hover as orange discs), `beam()` timber/slab between two points. Structures now render with `structMat()`: the shared vertex material with wrapped direct lighting (wrap 0.42, same idea as the character rig) so moon-shadow faces are a mid blue-grey instead of black; shadow maps and the warm point light unchanged. Church rebuilt as a real ruin: nave walls are `ruinWall` columns with 3–7 stepped jagged heights per wall (breach on the camera side, the far wall's rear half at ~40 %), lancets survive only where the wall stands; no roof slab — nine A-frame rafter pairs (collars, ties, two snapped, two fallen into the nave, a sagging broken ridge, purlins) with slates clinging to the rear bays; 1.3 × 1.5 m stepped buttresses on every bay (lower stages only at the breaches); crow-stepped west gable with portal + great west window the sky shows through, fallen right shoulder; porch with a two-order arch; tower in three set-off stages with clasping buttresses, blind arcading, twin belfry lancets, corbel table, broken parapet and a collapsed-spire stump with timbers (its tip lies on the forecourt); tumbled ashlar at the wall feet, east burial plot on the measured slope (headstones, crosses, flat slabs, a toppled cross, stub boundary wall), darker gravestones / paving. Wall runs at `mul 0.64`, trim 0.76 (ruin 0.66, fort 0.7). Fort: causeway posts and curtain sconces are halo-free, wayside campfire moved to the left of the approach (it sat where the new fort camera stands). Ruin: gable rebuilt as a `ruinWall` with a fallen shoulder, buttresses flanking the great lancet on the lake face, ashlar at its foot.
- `world/POI.js` — NEW `ruinWall(k, L, t, openings, top(u), color, o)` (quantised-course columns following a height profile, stacked pointed openings, jambs stay as piers through a break, arch heads only where both springers stand, per-column jitter below `o.full`), `blindArcade()`, `corbelTable()`; `rubble({ tilt })`, `brokenWall({ mul })`, `torch` / `brazier` pass `halo` through; `graveyard` tints darkened (0.62–0.82); sconce flames slightly smaller.
- `world/Pose.js` — NEW: `composeChurchPose` (player on the grass east of the forecourt, 3/4 view from the front-right at dist 7.4, camera tilted up 2° so the porch and gable are moonlit, the breached side wall and buttresses in shadow, tower behind; the grace beam is behind the camera), `composeFortPose` (east flank of the approach below the plateau, gatehouse / curtain / corner tower against the sky, grace behind the camera's shoulder), `composeRuinPose` (high ground above the stairs, the whole roofless abbey on its promontory against the misty basin, grace outside the left edge); `settleHud()` forces a style flush after Debug's `hideTitle()` so the day card cannot fade through a capture.

### cross-folder (architecture builder)
- `core/Debug.js` — `church` / `fort` / `ruin` poses delegate to `world/Pose.js` (same pattern as combat / ring / boss); import added, no other change.
- Note for `ui/HUD.js`: `hideTitle()` sets `transition: none`, removes the class and restores the transition in a rAF without a layout flush in between, so the browser may still animate the fade (a faint "DAY I" ghost appeared in captures); reading `offsetHeight` before the rAF fixes it.

## Gauntlet round 3 — night ring (2026-08-21)

Critic gap: the wall was a knee-high strip of blue flame with plain sky above, the rain fell on the player as hard as at the wall, the ground was a flat grey gradient (the pose stood inside the flattened Central Plain arena).

### run
- `run/Ring.js` — wall shells rebuilt as a storm veil with vertical volume: deep-navy body with a fresnel density (0.84 facing the camera → 0.98 where the wall curves away) that dissolves over the bottom ~3-6 m (noisy `footFade`, so the veil never cuts the terrain — the fire and pool own the contact) and loses a ragged top at 90-230 m; on it, three layers of falling light (tall 10-32 m curtains, mid streaks, fine needles, all sparse — ≤ 16 % of world-space columns lit so the half-res bloom no longer washes the frame — plus a rare bloom-bright layer), upward-drifting nebula cells and a rim band at the foot. Fire rewritten: tongue envelope 4.5-18.5 m by patch (`hmax`), crisp edges (`soft` widens with distance), fine licking detail, a broken-up base (never a solid band), base-to-tip radiance gradient (saturated blue → violet tips) and a thin pale core only in the densest filaments; skirt fire layers thinned (0.28 / 0.22) so the three layers no longer stack to white; fire/clump maths skipped above 22 m. Near-camera rain replaced by a rain curtain (1100 streaks, one draw call) confined to a band about the wall (radial offset `u·|u|·9 + 1 m`, ±48 m of arc around the player's bearing) — the player stands in dry air facing the curtain. World hook: `band` capped at 1.1 and coloured with the saturated fire body (`uRingF`) instead of the pale violet mix (no more white ground at the foot); `beyond` grade now sinks into a cold night fog with distance past the wall (`pall = 1 - exp(-0.04·d)`); NEW fresnel rim from the fire on anything standing within ~20 m of the wall (trunks, rocks, the player's hood; up-facing ground excluded); `smoothstep` calls with reversed edges replaced. Point light 24 cd, near-pure fire colour. Ground pool dimmer (0.18) with a faint cold pall beyond; annulus rows extended to +34 m. Motes re-clustered: half are fire sparks within ±3 m of the line, sparkles only 0-24 m inside, brighter gold embers.
- `run/Pose.js` — composition moved off the flat arena onto the grassy overlook crest (146, 208) facing north along the contour (camera behind on level turf, so no terrain pull-in; the wall 12 m ahead where the ground falls away, the dead trees of the valley beyond it lit blue, the hero tree's trunk and branches framing the right); pose-only `setOrbit(..., side 1.2)` sits the player left of centre. `RING_POSE` is an exported (mutable) config so a capture script can tune the composition by importing the page's own module instance (`performance.getEntriesByType('resource')` gives the `?t=` URL Vite served). Still holds the shrink at 0:42 for the HUD countdown + "THE NIGHT DRAWS NEAR".

### notes for other owners
- `tools/screenshot.mjs` dies with `Cannot read properties of undefined (reading 'state')` in its fps phase when Vite reloads the page mid-run (any builder saving a file); rerun when the tree is quiet.

## Gauntlet round 3 — field bosses (2026-08-21)

Critic gap: flat tan boss (no rider / mount separation, no rim or specular), un-joined paving slabs over a blown-out gold telegraph disc, player a black cutout with no grounding shadow.

### entity/bosses
- `bosses/Paving.js` — NEW continuous paved arena floor: one terrain-conforming decal mesh (draws first among transparents, no depth write, so contact blobs and the telegraph sit on it) whose fragment shader lays jittered running-bond flagstones with grout lines, joint occlusion, chipped edges, per-slab value / temperature jitter, a per-slab normal tilt (each stone takes the low moon at its own value), damp hollows, lichen, dust, hairline cracks, missing slabs showing packed dirt and a ragged fade into the earth; receives the moon shadow (the player's long shadow now grounds it). One draw call.
- `bosses/BossArena.js` — scattered box flagstones replaced by the Paving decal (r 11) plus nine heaved slabs at the rim for relief and fewer, smaller loose stones; the ruined gate moved off the fight axis (moonA + 0.62) so both piers and the broken lintel stand beside the boss against the sky, the gibbet swung to the other flank (moonA − 0.5) as a silhouette; brazier flames are registered in `limveld.fires` so the world's roaming torch point light (Grace.update) and Combat's rising embers follow the arena.
- `bosses/BossRig.js` — `armourMats({ warm })`: [cloth (matte flat), hide (matte smooth), ember, plate (flat facets, roughness 0.42 / metalness 0.72 so moon and brazier glints land per facet)], each with its own cool-rim / sky-fill / key-wrap uniforms plus a warm torch rim keyed to a world direction (`set.setWarmDir(v)`). `bossMats()` unchanged for the Demon.
- `bosses/Sentinel.js` — two-tone palette: rider in blued steel (steelDark-derived) edged with brass (gold/iron mix: helm bands, crest fin, pauldron edging + spikes, cops, breastplate ridge, shield rim / boss / cross, halberd bands), mount in near-black hide, deeper crimson caparison with brass hem, leather tack, steel crinet plates with brass rivets, brass chanfron. Plate tints route to the specular slot automatically (`PLATE` set). `updateFx` points the warm rim at the nearest brazier. Body telegraph glow cut to 0.02 (this build's emissive path is far hotter than its numbers — a 0.034 emissive paints the whole rig) and the ember light to 0.005 × at 6.8 m so the halberd head glows without gilding the shaft.
- `bosses/Telegraph.js` — ring redrawn as a thin soft rim (~2.5 % of the radius) with a faint halo, eight short rotating ticks, a whisper of fill whose thin leading edge fades into the rim as the wind-up completes (no more double ring), flash on impact; sits 0.1 m up so it lies on the paving. Sentinel ring alpha 0.4.
- `bosses/Pose.js` — calls `graces.update(0)` after `finish()` so the torch point light is parked on the nearest arena brazier for the capture (it stayed at the spawn grace before).
- Note for other owners: three's emissive in this pipeline reads ~10× hotter than expected (a 0.034 linear emissive turns the hero bright red in a probe) — anything driving `Entity.glow` / hit flash should assume that.

## Gauntlet round 2 — field bosses (2026-08-21)

### entity/bosses
- `bosses/Sentinel.js` — NEW day-1 field boss "Gate Sentinel": giant mounted knight (~4.6 m, 7 m with the halberd) on a barded black warhorse, one skeleton (rider bones hang off the horse's spine) on the shared RigBuilder/Animator; crinet-plated neck, gilt chanfron + brow spike, caparison with ragged hem, peytral, saddle/stirrups, rider in oxidised gilt (layered spiked pauldrons, winged great helm with crest fin + plume, crimson cape, 1.7 m round shield, banded halberd with crescent axe, hook, spike and pennant); slot-2 ember material (halberd head, eye slits, visor) heats gold on wind-ups. Clips: idle, run (trot→canter gait), roar (rears), alert, heavy (rearing overhead chop: ground ring + slam), light1/2 (sweeps), charge (lance-levelled trample with root motion + hoof dust), hit, stagger, death (folds, topples on its side). Moveset: sweeps / rearing chop / charge from range; phase 2 at half health.
- `bosses/HornedDemon.js` — day-2 boss kept; telegraph glow/heat/ring toned down (was a red-lit toy); `pickAttack` now measures the player distance itself (Enemy calls it without arguments, so the distance branches never fired).
- `bosses/Boss.js` — per-boss `heatScale`, `ringAlpha`, `emberBase`; the ember point light, telegraph ring and slam flash use the boss's `glowColor` (gold for the Sentinel, ember for the Demon).
- `bosses/Telegraph.js` — `show()` takes an alpha; ring shader is now a crisp rim + ticks + faint fill instead of a flooded disc.
- `bosses/BossArena.js` — cracked flagstone paving (r 10.5), ruined gate (two piers + broken lintel) in the column gap toward the moon, four low broken wall stubs, gibbet moved beside the gate.
- `bosses/index.js` — roster: day 1 Sentinel, day 2 Gravehorn Demon (`FIELD_BOSSES`, `createBoss(game, day, o)`).
- `bosses/Pose.js` — `boss` pose recomposed on the bar: low camera (pitch −0.13, dist 4.6) behind the guarding player, lock-on style shoulder offset (player left of centre; restored by the next camera `snap()`), the Sentinel facing the camera mid rearing chop wind-up (k = 0.9) with the halberd cocked across the sky, the day's final ring (r 115) closed around the arena for the rain-of-night backdrop, pre-aged hoof dust + brazier ember columns, boss reveal + bar.

### cross-folder (bosses builder)
- `run/Expedition.js` — `spawnBoss()` builds days 1–2 through `createBoss()` (day 3 keeps the Nightlord placeholder Soldier); `spawnDebug('boss')` does the same for the current day.
- `core/Debug.js` — `boss` pose delegates to `entity/bosses/Pose.js` (`composeBossPose`).
- Known: arena brazier glow flicker pauses once the boss is gone (only `Boss.updateFx` ticks the dressing).

## Gauntlet round 2 — night ring (2026-08-21)

### run
- `run/Ring.js` — rewritten as a storm front: the wall is a dark navy body (near-opaque at the foot, ragged top, luminous blue cloud cells drifting up it, three subdued rain-streak layers bunched into curtains) instead of a bright violet dash sheet; blue fire at the foot (`fireAt`: patch-gated tongues 2–9 m with hot pale-cyan cores that cross the bloom threshold, dark gaps between patches) drawn on the outer shell and on two skirt ribbons ±2.6 m for parallax; fire colours are palette-derived (`ring` / `ringRain` shifted via `offsetHSL`, no new hex); ground pool annulus now spans −32..+20 m with a flickering fire-lit pool at the line and sparse lit mist tendrils inside; world-light hook retuned (strong dappled flickering contact band ~0–6 m, a 20 m "reach" gradient, faint violet fill, stronger/colder desaturated grade beyond the wall); point light moved to flame height (60 cd, decay 1.5, flicker) for the player's rim; NEW near-camera rain (700 screen-aligned thin streaks in one additive draw call, density = ring proximity); motes split into fire sparks / drifting sparkles / gold embers / mist puffs with a 4 m camera keep-out. Inner shell skips the fire maths (uniform branch).
- `run/Pose.js` — NEW: `composeRingPose()` — player in the central meadow among dead trees, wall 12 m ahead, low camera (pitch −0.08, dist 11, yaw −0.3) so the grass is silhouetted against the fire and the wall towers; `ring.holdShrink(42)` so the HUD shows the closing-in countdown; fixed `ring.time` for repeatable flames. As before, the pose leaves the ring at r = 150 around the central meadow — poses captured after `ring` in the same session see that circle (the tool's default order captures `vista` first). Seen from outside the circle the wall's tangent silhouette now dissolves (`edgeFade`) instead of cutting a hard vertical edge.

### cross-folder (ring builder)
- `core/Debug.js` — the `ring` pose now delegates to `run/Pose.js` (`composeRingPose`), same pattern as `combat` / `boss`; no other change.

## Gauntlet round 2 — HUD & menus (2026-08-21)

### ui
- `ui/HUD.js` — restraint pass against the Nightreign frames: bar fills now run dark → bright toward the tip with a hot 2 px cap over a quiet translucent track (9/8/8 px), damage trails are short cream lags; level roundel redrawn as a glowing pale-blue sigil whose circles spill past the rim; the top-left panel spans the bar width with one fading hairline; rune count is plain digits on a faint panel with the day / phase line small and dim beneath; slots get cool hairline frames, a fuller sword and a painted golden flask (dim ghost when the player has none, count when `player.flasks` exists); art circles use softer rims; boss bar gets the same fill, an 8 px track and its sigil under the left end; reticle anchors at 60 % of the target's `height` (chest on humanoids and bosses). New `settleTrails(gap)` pose helper snaps both trails to sit `gap` behind the live value.
- `ui/Theme.js` — serif stack now leads with Palatino (lighter Garamond-like letterforms; Georgia remains the fallback). No webfont loads.
- Cross-folder (`core/Debug.js`, hud pose only): lock-on camera lowered and brought in (pitch 0.12, dist 6.6, +0.12 yaw) so the player fills the lower centre with the boss towering beyond; calls `hud.settleTrails(0.1)` before the final HUD update so the capture shows short HP / boss trails instead of full-width ones.

## Gauntlet round 2 — character & animation (2026-08-21)

### entity
- `entity/Humanoid.js` — rig rebuilt for the character piece: everything flat-faceted except the smooth head/neck/hands; trousers warmed toward leather, cloak cooled/darkened, boots a warmer leather with a pale folded cuff at mid-calf; shins swell at the calf and taper hard into the ankle onto a new wedge boot (`footGeo` returns upper + sole slab, rounded 10-point sections, heel, upturned toe); bracers are lofts that narrow into the wrist (no flat caps) with raised straps; bigger bevelled mitten fists with a knuckle ridge; jagged tunic hem (`mantleGeo` gained a z-scale); hood slimmed with a drooping peak and a wider brim; arms lengthened (elbow −0.30, wrist −0.285). Run clip retuned: lean lives in the spine (pivot pitch halved), swing knee drifts outward, sword arm swings less with the blade tipping forward on the fore-swing, toe skims the ground at toe-off; `runCadence()` + exported `HERO_SPEED` and a computed `RUN_PH0` put the hero frame (left knee drive, right leg trailing, blade forward-down) on the pose's 40-step settle. Cloak: narrower at the top, hem lifts clear of the legs at a sprint with a mid-length sag and deeper folds; lift floor 0.5 for any run. Contact shadows: a third faint wide oval under the pelvis, foot ovals fade over 0.45 m, opacity 0.85. Rim 1.4 / wrap 0.42.
- `entity/Player.js` — passes a terrain `ground(x, z, n)` sampler to `createHumanoid` (the per-foot contact shadows were never positioned before); `yaw` is now an accessor that writes `object3d.rotation.y` immediately so a pose that sets yaw without stepping physics renders facing the right way.
- `entity/Camera.js` — `setOrbit(yaw, pitch, dist, side = 0, lift = 0)`: pose-only lateral pivot shift and pivot height delta, reset by every `setOrbit` call (gameplay unchanged).
- Cross-folder: `core/Debug.js` `character` pose — sets `anim.ctx.speed = HERO_SPEED`, camera pitch 0.14 / dist 3.1 / yawOffset 0.95, then `setOrbit(..., -0.55, -0.55)` to frame the hero right of centre under the moon, clear of both HUD clusters; the old `anim.t = 0.27` nudge is gone.

## Gauntlet round 2 — vista & atmosphere (2026-08-21)

Critic gap: flat tan ground plane with repeated identical tufts, featureless area under the character; bar is Ashen steam_04 (layered ridges, lit sky, scale cues, ground value variation).

### render
- `render/Style.js` — terrain turf darker/olive so the ochre tufts carry the highlights (`grass`, `grassDark`, `grassPale`, `straw`, `dirt`, `damp` retuned), `path` warmer, `outcrop` contrast up, `grassTuft` more ochre. No keys removed.
- `render/Atmosphere.js` — distance haze curve is now quadratic (`pow 2.0`, density 0.00245 → ~6 % at 100 m, 21 % at 200 m, 41 % at 300 m, 70 % at 450 m) so near / mid / far / backdrop read as four value bands; valley mist a little thinner and faster-falling with altitude (ridge crests stay clear).

### world
- `world/Terrain.js` — `_faceColor` rebuilt for value structure: broad + fine + facet-scale (9 m) mottling, dry straw only in patches on convex moonlit ground, ragged two-octave bare-earth `patchMask` (seed chosen so the overlook is turf with one dirt pocket in view), `scuffMask`, stronger moon-facing step (0.74 + 0.5·facing) and ±15 % per-face jitter; `groundMask(x,z)` exported for prop thinning; path ruts darker; rim mountains gained ridged spikes for a notched far crest.
- `world/Props.js` — three tuft silhouettes (tall fountain / short cushion / broad hay clump) with thicker blades; clumps of 5–12 sharing a tint (wide cool-olive → straw range), thinned on bare ground, the track and `Limveld.trampled` zones, `grass` landmarks place fixed clumps; crags rebuilt as leaning stacked-slab piles with twin spines and fallen blocks (pale ledges); `tree` landmarks; meadow `density`; small-stone litter along every track and in meadows (size-biased small, sunk).
- `world/Limveld.js` — track re-routed to enter the vista frame bottom-right and pass 4–6 m beside the player (leading line to the brazier); dead-tree lines on the ruin ridge, the near spur and the far cliff band; valley-floor trees + boulders along the track; foreground stone fan and a jittered grid of grass clumps across the vista camera's near wedge; `trampled` list + `trampledAt()` (grass-only soft clearings).

## Gauntlet round 2 — churches, forts & ruins (2026-08-21)

### world
- `world/Structures.js` — Kit rebuilt as the one-draw-call site builder: frame stack (`push/pop`), `wedge` (set-off caps), `fire()` (registers a flame for Atmosphere AND bakes warm light into the merged vertex colours, normal-weighted), boxes subdivided every `seg` m so baked light varies across big faces, per-vertex mottle + damp base course + `mul` tint so wall runs sit darker than pale trim, `gothicWall` (stacked openings, gap-free arch heads for wide portals). Church: plinth + paved forecourt with cheek-walled stairs, piers and corner braziers; 10.5×22 nave with mullioned twin-lancet windows and string courses; crow-stepped gable (oculus, twin lancets, cross) whose right shoulder has fallen so the open rafters / ties / collars / purlins of the collapsed front roof show through; jagged breach in the near wall with fallen masonry and slates; gabled porch with a two-order pointed arch, corner buttresses and sconces; stepped buttresses on every bay; 17 m west tower (string courses, clasping buttresses, belfry lancets, corbel table, merlons, pinnacles, 11 m spire — stump when `ruined`); lit altar, pews, rubble, lit rear windows; walled graveyard + open rows by the approach. The building stands 8 m behind the POI centre so the whole gable fits the fixed church pose. Fort: gatehouse with twin towers, nested gate arch, half-raised portcullis, machicolation, torches + banners, corbelled parapets, arrow slits, round corner towers, 20 m keep with lit lancets and a 38 m tower, flagstone road with stakes, gibbet, curtain torches, a measured-profile stone causeway with torch posts down the hill and a wayside campfire. Ruin: raised floor, jagged gable wall with a great mullioned lancet, diminishing arcade, column rows, tower stub, sarcophagus, two braziers. Catacomb / camp unchanged apart from palette-derived dark tints (no new hex). Kits are ~11k / 17k / 4k tris.
- `world/POI.js` — now wired in (it was an orphan referencing a Kit API that did not exist): helpers draw into the Kit frame stack; added `lancet()` (pointed dark recess slab for windows / slits / belfries) and `graveyard({ mix })` shares; `brokenWall` profiles drive the crow-stepped and fallen gables.

### notes for other owners
- `world/Limveld.js`: the church kit now spans ±17 m (forecourt + stairs to local z ≈ +20); a `flattenDisk` radius of 18 for church POIs (currently 14) would keep the forecourt edge on flat ground. A `ground(lx, lz)` callback passed into the kit options would let paths / graves / yard walls conform to terrain beyond the flat disc (the church approach slabs are currently buried where the ground rises ~0.5 m toward the grace). The fort causeway's height profile is hard-coded from a terrain probe of the current seed (the hill falls 9 m between local z 36 and 58); re-measure if the fort moves or the heightfield changes.
- `core/Debug.js`: the `fort` pose camera sits 9 m below the plateau looking up; anything in the right half of that frame must stay under ~2 m above the slope to be visible.

## Gauntlet round 2 — combat feel (2026-08-21)

### combat
- `combat/Arena.js` — trampled-ground decal widened to r 13.5 m (the fight no longer stands on its edge) and its albedo lifted to a mid tan-grey (path/sand mix) so it reads under moonlight: three-scale albedo breakup (damp hollows, pale dry-dust crowns, grit), sparse jittered single-boot prints clustered along a trodden band (no more tiled pairs), soft ash drifts + a few flakes, fewer ember specks; clutter kit bakes two-tone facet shading (up-facing faces take a cool sky fill, undersides darken) and scatters more debris (stones to 12 m, charcoal, 70 pale straw stalks, gnawed bones).
- `combat/Combat.js` — impact rework: 34 small hot→orange shard streaks (longer stretch), hanging hot motes, falling chips, tight core, small warm halo + ring, two slash streaks; impact light much weaker (2.4 cd) and placed 1.3 m back toward the attacker so it rims the target instead of bleaching it; dust earthier, lower, fewer puffs; `afterPose` restore hook run on the first live update (undoes pose-only camera tweaks).
- `combat/Trail.js` — trail life 0.13 s; ribbon body kept faint with a bright tip band (a brighter body piled additively into a blob where the inner samples bunch near the hand).
- `combat/Pose.js` — composition rebuilt: exported tunable `POSE` (stand radius / facing / orbit / swing fraction / enemy offsets / camera lock / shoulder); the struck Soldier is planted *on the hero's blade* (contact from `boneToWorld` + `bladePoints` after the swing snaps, with a chest-height fallback) so sparks, cut and recoil line up; hero stands 8.5 m from the fire so the camera orbits outside the tent ring; no camera lock (Camera.update keeps blending toward a lock target while posing, so the frame depended on frame rate) — explicit orbit + a negative shoulder offset puts the hero right of centre, fire and wolf far left, knight right; warm struck tint on the soldier.

### entity
- `entity/Enemy.js` — `takeHit` override: enemies get a brief weak warm (or cool, when guarding) struck tint instead of the base white-out — the white flash was what turned a 30 %-value figure into the critic's "pale grey blob"; blade telegraph heat reduced to a dull red (0.015 g + 0.11 g², flash 1.2 g²).
- `entity/enemies/EnemyRig.js` — soldier / knight palettes darkened a step (dark steel plate, deep wine knight cloth so firelight does not turn it salmon).

### notes for other owners
- `world/Structures.js` — the round-1 combat builder left two-tone tents (patched panels, ridge/end poles, guy ropes, pegs) and banded crates in `camp()` with a code comment but no CHANGELOG line; recorded here.
- `entity/Camera.js` — `update()` runs while `game.posing`; any pose that sets `cameraCtl.lockTarget` drifts toward the target over the screenshot wait (frame-rate dependent). Poses should leave the lock null or accept the drift.
- Scratch capture tooling used this round (not committed): a retrying Playwright capture that re-boots when a parallel edit reloads the page — `tools/screenshot.mjs` dies on those reloads.

## Gauntlet round 1 — vista & atmosphere (2026-08-21)

### render
- `render/Style.js` — palette re-tuned to a desaturated slate night (pale misty horizon, dark zenith, olive/tan ground, tan grass, darker trees); new keys `mist`, `cloud`, `monolith`, `monolithDark`. No keys removed.
- `render/Atmosphere.js` — overrides three's fog shader chunks at import with height + distance fog (mist pools in valleys, haze builds with a power curve, colour = horizon sky brightening toward the moon) so every fogged material gets it for free; sky dome now has a large limb-darkened moon with maria, sparse varied stars, streaky cloud bands; lights rebalanced (neutral hemi, warm ground bounce, stronger cool moon directional, shadow radius 3); builds the distant glows (below).
- `render/Glows.js` — NEW: one additive billboard mesh (single draw call, vertex-shader billboarding + flicker) with a warm glow at every campfire / brazier / grace so warm accents read across the vista.
- `render/Postfx.js` — vignette pass now also lifts blacks with a cool slate tint and desaturates 6 % (muted film look); vignette strength 0.42.

### world
- `world/Terrain.js` — heightFn gained ridged crests, facet-scale roughness, art-directed `LANDFORMS` (overlook knoll, misty valley, ruin ridge, far-shore hills), a two-band jagged rim; lake moved to (-260, 60); `shaveDisk()` stamp (lowers bumps only — camera clearance without flattening hollows); per-face colours now bake moon-facing value steps + dry pale grass on crests; `_buildBackdrop()` ring of fogged peaks at 620–1000 m (third silhouette band); water is a pale sky-toned sheet.
- `world/Limveld.js` — new ridge-top `Overlook Ruins` POI (pushed last, so `poi(type, i)` indices are unchanged) with two iron braziers; `braziers`, `landmarks` (hero trees, monoliths, rocks), `meadows` (dense grass zones) and `clearings` (prop keep-out; `isClear` honours them) lists; `meadowBoost(x,z)`; Lakeside Ruins → (-300, 230) and Western Camp → (-330, -150) to follow the lake; overlook platform shaved at (153, 212).
- `world/Props.js` — added hero trees (2 variants, landmarks + hilltop scatter), standing stones, ruin fragments (tall/short columns, pointed arches, wall stubs in ~30 clusters on high ground), braziers with instanced emissive flames, meadow passes (dense tufts + small rocks); grass tufts now double-winding triangles with up-facing normals on a smooth-shaded material (lit like the ground, no black fins); `_instance` gained `bucket:false` for sparse types. ~545 k tris / ~265 draw calls (incl. shadow + post) in the vista.

### notes for other owners
- `run/Ring.js`: the day-1 wall (r = 600) ignores fog and draws a saturated violet line + rain dashes across the distant mountains in the vista — consider fading it with the scene fog / distance so the far-band reads cleanly.
- Vite hot reloads from parallel edits can interrupt `tools/screenshot.mjs` and `tools/smoke.mjs` mid-run (`window.__game` undefined); rerun when the tree is quiet.

## v0.1.0 — first playable vertical slice (2026-08-21)

Run: `npx vite --port 5173` → http://localhost:5173. Screenshots: `node tools/screenshot.mjs [pose...]`.
Smoke test: `node tools/smoke.mjs` (27 checks, drives the loop headlessly through `window.__game`).

### root
- `index.html` — full-window canvas `#c`, `#hud` + `#menu` overlay roots, dark background, no external assets.
- `src/main.js` — boots `new Game(canvas).start()`.

### core
- `core/Game.js` — renderer (pixel ratio 1, ACES, PCF shadows), fixed 1/60 step with dt clamp 1/30 and hit-stop time scale, state machine HUB → EXPEDITION → RESULTS, entity list, quality switch, rolling fps, `manualSim` for tests.
- `core/Input.js` — keyboard/mouse/gamepad → named actions (held + edge), pointer lock helper that never throws.
- `core/Events.js` — on/off/emit bus; events used: player:hit, enemy:hit, enemy:died, runes:changed, boss:start, boss:died, day:changed, ring:phase, grace:rest, levelup, player:died, run:start, run:won, run:lost. Not yet emitted: boss:phase, loot:pickup.
- `core/Rng.js` — mulberry32 with float/int/range/pick/chance/fork.
- `core/Debug.js` — `window.__game`: teleport, setTime, spawn(soldier|guard|wolf|boss|nightlord), killAll, setFps, setQuality, screenshotPose(vista|combat|grace|ring|boss|church|catacomb|hub), fps, ready, state, startExpedition, resume, setManual, advance. (combat builder: the `combat` pose body now delegates to `combat/Pose.js`.) (grace builder) `grace` pose recomposed: low camera (pitch −0.08, dist 5) behind the kneeling player 2.8 m from the central grace, beam rising behind the silhouette, rare chest in frame; calls `graces.update(0)` so the warm light + prompt are current.

### render
- `render/Style.js` — PALETTE (all hex lives here), NIGHTFARER_COLORS, flatMat/vertexMat/charMats/emissive factories, colour helpers.
- `render/Atmosphere.js` — gradient sky dome shader with moon + hashed stars, FogExp2 matched to horizon, hemisphere + ambient + moon directional with one 2048 shadow map re-centred when the player moves > 2 m, one roaming warm point light, per-day tint.
- `render/Postfx.js` — EffectComposer (MSAA x4 HalfFloat RT): render → half-res UnrealBloom → vignette/damage-flash → OutputPass; 'low' quality bypasses it.
- `render/Particles.js` — GPU billboard particle system (burst / home / orbit modes) used for sparks, rune motes and grace motes. (Added beyond the ARCHITECTURE list; lives in render/.)

### world
- `world/Terrain.js` — seeded fbm heightfield 1200 m, 8×8 chunks × 48 segs with 2-level LOD (swap at 230 m), per-face vertex colours by height/slope/noise, flattenDisk/raiseDome stamps, getHeight/getNormal bilinear, lake plane in a basin, rim mountains.
- `world/Limveld.js` — seeded layout: spawn, 8 graces, 2 churches (one ruined), 2 ruins, fort, catacomb entrance in a raised hillside, 2 camps, 4 wolf dens, 3 flat boss arenas; enemy spawn table; campfire list for the warm light.
- `world/Structures.js` — Kit builder (boxes/cylinders/cones/gable prisms, pointed-arch walls, baked AO) and kits: church, fort, ruin, catacombEntrance, camp. One mesh per structure (+1 emissive flame mesh).
- `world/Props.js` — instanced dead trees (2 variants), rocks, grass tufts, gravestones, bucketed into a 4×4 region grid for culling; terrain rules (no water, slope/height limits, POI exclusion, forest/meadow noise).

### entity
- `entity/Entity.js` — transform, hp/stamina/poise, terrain physics + bounds, takeHit (i-frames, guard, knockback, poise → stagger, death), hit-flash / telegraph emissive.
- `entity/Humanoid.js` — RigBuilder (primitives → one SkinnedMesh; `part()` now takes blend/skirt weights + per-vertex shadeFn for smooth joints and baked AO), Animator (pose blending, whole-body pivot, `onUpdate` hook, `settle()` snaps to the clip target), designed costume: tapered blended limbs with ankle bones, tunic/belt/pouch/strap, two-layer jagged leather mantle, hood with peak (or faceted helm / hair), skin hands, wedge boots; cloak + scarf as one swaying mesh (speed-driven lift, folds, torn hem, own depth material so shadows sway); character shader hook (cool fresnel rim + sky fill + key-light wrap, matte); fog-aware contact-shadow blob. Clips: idle, run/sprint (lean, counter-rotation, toe-off), roll, light1-3, heavy, hit, stagger, death, guard, rest, alert.
- `entity/Player.js` — camera-relative movement with accel/decel, sprint, roll (i-frames 0.42 s), 3-hit light combo + heavy with input buffering, skill (lunge) and ultimate (burst) placeholders on FP/cooldown, stamina regen delay, lock-on, level scaling, respawn, rest. Always hooded + cloaked (Wylder gets the palette-red scarf, others their accent); contact shadow is laid onto the terrain normal each frame and on teleport.
- `entity/Camera.js` — third-person orbit (pointer lock), smooth follow, terrain collision pull-in, lock-on framing, shake, sprint FOV, hub orbit mode, over-the-shoulder pivot offset (0.28 m, character sits left of centre).
- `entity/Enemy.js` — FSM idle/patrol/alert/chase/attack/recover/stagger/hit/guard/dead; telegraph = faint body glow (`glowScale` per enemy) + blade heating in its own emissive material group, white blade flash on release, step dust on the first active frame, knockback dust on hurt/stagger/death; circle-strafe, combo follow-ups, separation, rune drop + enemy:died.
- `entity/enemies/EnemyRig.js` — armoured humanoid builder on the shared RigBuilder/Animator (same bone layout as createHumanoid, incl. neck/ankles): layered pauldrons, breastplate ridge + gorget, tassets over a cloth skirt, tabard, kettle hat or great helm with crest/plume, greaves, round shield with emblem, cape, blade in material slot 2 (emissive). ENEMY_COLORS tints (soldier/guard/knight/boss).
- `entity/enemies/Soldier.js` — sword + round shield, gambeson + half-plate, crested kettle hat (tiers 1/2 colourways), occasional guard; boss variant uses the knight kit with greatsword + cape.
- `entity/enemies/Knight.js` — NEW tier-2 heavy: great helm, plume, cape, two-handed greatsword, slow long telegraphs (`knight` moveset), 1.15× scale, high poise.
- `entity/enemies/Wolf.js` — redesigned silhouette: hunched shoulder hump, spine ridge spikes, mane, low wedge head with open jaw/teeth, big paws, bushy tail, pale chest; lighter fur; packs of 3 share aggro.
- Not built yet: Archer, Bat, Troll, `bosses/*` (real boss kits with phases).

### combat
- `combat/Combat.js` — swept arc sectors during active frames (one hit per target per attack), damage/poise, hit-stop + shake, rune payout; FX: weapon trails (pool of 3, auto-attached to any attacker with a bladed visual), stylized `impact()` (velocity-stretched spark streaks, chips, white core, soft glow, expanding ring, one shared PointLight pulse), `dust()` / `stepDust()` / `hurtDust()` ground puffs, ultimate burst.
- `combat/Fx.js` — FxPool: GPU quad particles with per-particle shape (streak / glow / ring / puff), drag, stretch along screen-space velocity, growth; additive or normal blending; CPU work only at spawn.
- `combat/Trail.js` — WeaponTrail ribbon (base→tip samples with GPU fade by birth time) + `boneToWorld()` for rigid points on skinned bones.
- `combat/Pose.js` — deterministic `combat` screenshot composition (camp gap, fire backlight, baked swing trail, recoiling Soldier, Knight heavy windup, crouched Wolf, warm-up render).
- `combat/Weapons.js` — weapon table (11 entries incl. knightSword), movesets (greatsword, sword, soldier, knight, wolf), SKILLS placeholders, TRAIL_SPAN + `bladePoints()` (outer-blade extents per visual). Rarity/scaling fields are present but unused by Loot (no Loot yet).

### run
- `run/Expedition.js` — 3 days × 270 s: explore → ring1 (60 s shrink) → explore → ring2 → boss; nested ring circles around a chosen arena; placeholder bosses (scaled Soldiers: Sentinel Knight, Hunter of the Bell, Nightlord 2.4× with 2700 hp); day advance on boss death, win → RESULTS, abandon → RESULTS; respawn 3.2 s after death; debug setTime/spawnDebug. Outside-ring damage tick reads the `player.outsideRing` flag Ring writes.
- `run/Ring.js` — "rain of night" weather front: two terrain-following shells (per-column ground height attribute, inner shell for parallax) with a premultiplied shader — dense dark veil that cools what lies beyond, broad x narrow falling curtains, three streak layers (needles / streaks / sparse bloom-bright), blue fire tongues + cores in patches at the base, rim glow, same pow-1.7 haze curve as Atmosphere so the far side fades; terrain-conforming rim strip that spills violet light on the ground inside and cools the ground outside; 600 orbit-mode motes (cold sparks, violet, golden embers, faint mist puffs) round-robin re-placed along the wall arc nearest the camera; per-frame it writes `player.ringDist` / `player.outsideRing` and pulls `scene.fog.color` toward the ring violet by proximity (reads `atmosphere.fogColor` as the base). Eased shrink; `distInside()` / `isOutside()`.
- `run/Grace.js` — grace round 1: branching golden hilt (tapered blade, curved guard arms with twigs, wrapped grip, knotted pommel) + golden roots along the terrain, one merged bronze-gold mesh; `LightPillars` (exported) — one-draw-call additive cylindrical-billboard beam shader (flame-shaped width, ridged upward filaments, wisp break-up toward the top, brightest band ~2.5 m up) + spherical flares; per site: 7.6 m beam, inner column, base flare, wide haze glow; `softDisc` (exported) terrain-conforming multi-ring ground glow (real radial falloff, no hard disc edge); 150 motes per site in three layers (tight spiral, ground drift, dissipating crown); warm light lifted to 2.6 m at ~13 cd so the pool stays gold; hosts `LootSystem` (`game.loot`) and updates it; `nearest()` no longer allocates.
- `run/Loot.js` — NEW: 12 chests (church ×2, fort gate + legendary keep chest, ruins ×2, catacomb, camps ×2, spawn and central-grace field chests) + 4 planted weapons at the wolf dens; rarities common/uncommon/rare/legendary with palette-derived glow colours (moon / ring / ringGlow / graceGlow), per-day weights, unopened loot re-rolls upward on `day:changed`; each item has a rarity pillar + flare + ground glow + 14 motes; `E` prompt ("Open Chest" / "Take <weapon>", defers to the grace prompt), pickup equips a rarity-scaled weapon from the table, pays runes, animates the lid open, extinguishes the glow, emits `loot:pickup`, shows a title card; `run:start` resets. 6 draw calls total (merged bodies, instanced lids, instanced weapons, glows, pillars, motes).
- Not built yet: `run/ShiftingEarth.js`.

### ui
- `ui/HUD.js` — Souls-style HUD v2: bevelled hairline bar frames with gold end caps, HP + boss delayed pale damage trails (JS-driven, 0.6 s hold), nightfarer/level line, day gauge (day-progress ring with night-closing arcs + ring radar) under the bars with the phase timer, top-centre compass strip (ring/grace/boss markers), chamfered weapon/skill/ultimate slots with conic cooldown sweeps, rune plate with gold rune glyph, boss bar with left-aligned name, Elden-Ring-style lower-third boss reveal (replaces the centre card for boss names; hides the small label meanwhile), ring reticle, keycap prompt. Visibility is class-driven (instant show, fade on hide) so captures never catch a mid-fade; names pass through `clean()` which strips dev suffixes like "(Placeholder)". Cross-folder edit by the ring builder: the `.h-warn` element also shows a violet "THE NIGHT DRAWS NEAR" when `player.ringDist < 25` (written by run/Ring.js).
- `ui/Menus.js` — shares `ui/Theme.js` (font, text shadow, keycap, ornament); panel gets hairline + inner gold line, divider gets the diamond ornament, hover uses palette gold. Logic unchanged.
- `ui/Theme.js` — NEW: palette-derived colour helpers (`alpha`, `mix`, `shade`), serif stack, text shadow, `chamfer()` clip-path and shared `.u-key` / `.u-orn` CSS for HUD and Menus (no hex outside Style.js).
- `ui/Map.js` — M toggles a heightmap render with POIs, graces, ring + next ring, player marker (sim paused while open).

### nightfarers / hub / meta / audio
- `nightfarers/index.js` — STUB roster: 8 names with hp/fp/stamina/weapon/desc; skills are shared placeholders in Weapons.SKILLS.
- `hub/Roundtable.js` — not built; the hub is the overlay menu in Menus over an orbiting camera.
- `meta/Relics.js`, `audio/Audio.js` — not built.

### tools
- `tools/screenshot.mjs` — Playwright (cached Chromium, SwiftShader with fallback), 1920×1080, starts Wylder, captures shots/<pose>.png, prints fps over 3 s, renderer.info and any console errors (exit 1 on errors).
- `tools/smoke.mjs` — deterministic headless gameplay test of the full loop (manual sim stepping).

### Known gaps
- Bosses are scaled Soldiers; no phases, no arena walls, no intro cinematic.
- Ironeye's bow, Recluse/Revenant staves use melee movesets (no projectiles).
- No relics, shifting earth, audio, or damage numbers (numbers are off by design). Loot pickups swap the player's weapon stats/moveset but not the rig's weapon visual (Player builds it once).
- No enemy-vs-terrain steering (enemies walk straight; terrain is open so they rarely get stuck).
- Headless SwiftShader fps is 2–3 at 1080p (expected); real-GPU 60 fps target is untested here.
